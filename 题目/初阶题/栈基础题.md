### （1）递归式的先序遍历一个n节点，深度为d的二叉树，需要栈空间的大小为___O(d)___。
#### 解释：在从根向下遍历中，每次先转移到左子树上，而右边则需要暂存起来，因此，最多需要的暂存空间需要d个。
#### 因为二叉树并不一定是平衡的，也就是深度d！=logn，有可能d > > logn(远大于)，所以栈大小应该是O（d）
### （2）将一个递归算法改为对应的非递归算法时，通常需要使用（  栈）。
#### 递归的本质就是栈。栈可以用来消除递归，但消除递归不一定用栈，例如尾递归。
### （3）单链表实现的栈，栈顶指针为Top(仅仅是一个指针)，入栈一个P节点时，其操作步骤为：
#### B：p->next=Top->next;Top->next=p; 
#### C：p->next=Top;Top=p->next;
```
单链表的栈，栈顶就是第一个节点，这里可以有两种实现： 
  1、top节点表示真实的栈顶节点； 
  2、top节点只是一个标识，其next才是栈顶节点。 
  对于1，入栈操作是在top前插入节点：p->next = top; top = p; 
  对于2，入栈操作是在top后插入节点：p->next = top->next; top->next = p; 
  所以，选B。
```
### （4）堆栈溢出一般是由什么原因导致的？
### 1.循环的递归调用；2.大数据结构的局部变量
### （5）栈的常见应用：浏览器历史纪录，Android中的最近任务，函数调用，Activity的启动模式，CPU中栈的实现，Word自动保存，解析计算式，解析xml/json

